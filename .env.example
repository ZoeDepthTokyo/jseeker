# jSeeker Environment Configuration
# Copy this file to .env and fill in your values.
# The .env file is gitignored and must never be committed.

# ── Required ──────────────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-your-key-here

# ── Optional: GAIA integration ────────────────────────────────────────────────
# Leave empty to run jSeeker in standalone mode (no GAIA services required).
GAIA_ROOT=

# ── Optional: ATS auto-apply credentials ─────────────────────────────────────
# Used by the Playwright-based auto-apply pipeline.
# Leave empty to disable automated submission for that platform.
WORKDAY_EMAIL=
WORKDAY_PASSWORD=
GREENHOUSE_EMAIL=
GREENHOUSE_PASSWORD=
